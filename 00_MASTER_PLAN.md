# SPIDER GAME VER.2 — 최종 기준서

> 이 문서 하나만 읽고 새 세션에서 바로 작업을 시작할 수 있도록 쓰였다.
> 목표, 지금까지 한 것, 앞으로 할 것, 검증 방법이 전부 여기 있다.
>
> 개정 2026-09-09 (P2까지 완료 시점 · 레퍼런스 2편 분석 반영)
> **이 문서와 코드가 어긋나면 코드가 아니라 이 문서를 먼저 고친다.**

---

# 0. 새 세션용 요약 — 이것만 먼저 읽어라

## 0.1 지금 무엇을 만들고 있는가

**1인칭 스파이더맨 액션 게임.** 레퍼런스 영상 2편의 느낌 + PS4 스파이더맨의 손맛.

핵심은 하나다 — **모드가 없다.**
스윙하다가 그대로 적을 웹으로 끌어당겨 눈앞에서 주먹으로 치고, 벽에 묶고,
간판을 뜯어 던지고, 다시 줄을 걸고 날아간다. 그 사이에 전투 모드 전환이 없다.

## 0.2 지금 상태 (2026-09-09)

```
엔진        Unity 6.6 (6000.6.0f1) · URP 17.6.0 · Windows 데스크톱 타깃
진행        P0 ✅  P1 ✅  P2 🔶(코드 완료, 유니티 컴파일 검증 대기)
커밋        30073d0 P0 부트스트랩
            fb66e6e P1 놀이터 + 1인칭 카메라 + TuningConfig + 디버그 오버레이
            640e040 P2 단일 웹 물리
            462e314 듀얼 트랙 결정 + Ver.1 스윙 물리 이식   ← 최신
저장소      https://github.com/ebag44868-svg/Spiderman_ver.2 (main + backup/* 푸시됨)
테스트      EditMode 37개 (마지막 전체 통과 확인 시점: 462e314 직전)
```

## 0.3 새 세션이 가장 먼저 할 일

```
1. 이 문서를 끝까지 읽는다.
2. git log --oneline -5 로 실제 커밋 상태를 확인한다.
3. 유니티가 켜져 있는지 확인한다 (tasklist | findstr Unity.exe).
   꺼져 있으면 → 배치모드로 BuildTools.Verify + 테스트를 직접 돌린다 (§3.2)
   켜져 있으면 → ClaudeBridge로 명령을 보낸다 (§3.3)
4. 462e314에서 이식한 Ver.1 스윙 물리가 컴파일되는지, 테스트 37개가 통과하는지 먼저 확인한다.
   이게 P2의 미완 항목이다.
5. 그다음 P3(AUTO ANCHOR V2)로 간다. 이게 이 게임의 정체성이다.
```

---

# 1. 확정된 환경

```
Unity            6.6 (6000.6.0f1)
경로             C:\Program Files\Unity\Hub\Editor\6000.6.0f1\Editor\Unity.exe
패키지           URP 17.6.0 · Input System 1.20.0 · Cinemachine 6.6.0
                 Test Framework 1.8.0 · uGUI 2.6.0
Hub              C:\Program Files\Unity Hub\Unity Hub.exe

Ver.2 폴더       C:\Users\SAMSUNG\OneDrive\Desktop\Spiderman_Ver2
Ver.2 저장소     https://github.com/ebag44868-svg/Spiderman_ver.2

Ver.1 폴더       C:\Users\SAMSUNG\OneDrive\Desktop\spider man   (감각 실험장 — §6)
Ver.1 저장소     https://github.com/ebag44868-svg/spider-man-game
Ver.1 동결 태그  v1.0-final = 커밋 fe4a5cb (main) — 절대 건드리지 않는다

레퍼런스 A       C:\Users\SAMSUNG\OneDrive\Desktop\문서\카카오톡 받은 파일\스파이더맨 래퍼런스.mp4
                 46.8초 · 960x720 · 24fps · 1인칭 스윙
레퍼런스 B       C:\Users\SAMSUNG\Downloads\ScreenRecording_09-07-2026%2014-43-37_1.mp4
                 50.1초 · 682x512 · 30fps · 1인칭 몸 + 옥상 전투
                 ※ 파일명의 %20은 실제 문자다. 인용부호로 감싸야 한다.

ffmpeg           설치됨 · PATH에 있음
node             v26.3.0 (Ver.1 실험장 구동용)
git              2.54.0.windows.1   ※ gh CLI 없음 — 푸시는 자격증명 창이 뜰 수 있다
플랫폼           Windows 11 · PowerShell 주 · Bash 병용 · iOS 빌드 불가
```

**주 타깃은 Windows 데스크톱 빌드.** WebGL은 P12에서 검토한다.

⚠️ 프로젝트가 OneDrive 동기화 폴더 안에 있다. `Library/`는 수 GB가 되고 유니티가 계속 쓴다.
OneDrive에는 폴더 단위 제외 기능이 없어서, 확실한 해법은 프로젝트를 OneDrive 밖으로
옮기는 것뿐이다. 아직 안 옮겼다. 임포트가 깨지거나 느려지면 이걸 먼저 의심한다.

---

# 2. 작업 규칙 (사용자 선호 — 반드시 지킬 것)

1. **호칭은 "형님", 극존칭을 쓴다.**
2. **토큰을 아낀다.** 사용자는 만성적인 주간 한도 부족을 겪는다.
   불필요한 재확인 · 장황한 설명 · 반복 읽기 금지.
3. **한 번에 하나씩.** 큰 변경 전 백업 브랜치. **백업 브랜치는 절대 삭제하지 않는다.**
4. **보고는 하나의 연속된 복붙 가능한 코드블록으로.** 여러 블록으로 쪼개지 않는다.
5. **사용자 조작을 최소화한다.** 아래 §3의 자동화 경로를 먼저 다 쓴 뒤에만 요청한다.
   요청할 때는 짧게:
   ```
   USER ACTION
   1. ...
   DONE CRITERIA
   ...
   ```
6. **조작법이나 기능을 추가하면 인게임 도움말(F1)에도 반드시 같이 넣는다.**
7. **모르는 것을 아는 척하지 않는다.** 확인할 수 있으면 확인하고 말한다.
   애매하면 추론하지 말고 질문한다.
8. **한글·이스케이프가 섞인 문자열은 치환 스크립트로 만지지 않는다.**
   (2026-09-09에 `\n`이 실제 줄바꿈으로 들어가 C# 문자열이 깨진 사고가 있었다. Edit 도구를 쓴다.)

### 단계 보고 형식
```
[PHASE REPORT]
1. 목표          2. 작업 전 상태     3. Backup
4. 변경 파일     5. 실제 변경        6. 기존 기능 영향
7. 자동 테스트   8. Unity Play 확인  9. 레퍼런스 비교
10. 문제         11. 남은 위험       12. 사용자가 할 것
13. 완료 기준    14. Commit          15. 다음 단계
```

---

# 3. Claude가 직접 할 수 있는 것 — "딸깍이면 구현되도록"

**목표: 형님이 하는 일은 Play 버튼과 감각 판정뿐.** 나머지는 전부 Claude가 한다.
아래 세 경로가 그걸 위해 만들어졌다. 새 세션은 이걸 반드시 활용한다.

## 3.1 Claude가 직접 하는 것

- C# 파일 작성/수정 (런타임 + 에디터 툴)
- **씬 생성, 오브젝트 배치, 컴포넌트 추가, 참조 연결** — 전부 C# 에디터 스크립트로
- 프리팹 생성 (`PrefabUtility.SaveAsPrefabAsset`)
- 패키지 설치 (`UnityEditor.PackageManager.Client`)
- 프로젝트 설정 변경 (`PlayerSettings` API, `SerializedObject`)
- 컴파일 확인 · 로그 읽기 · **EditMode 테스트 실행**
- Ver.1(Three.js) 실행 · 브라우저 직접 조작 · 스크린샷 · 측정
- ffmpeg으로 레퍼런스 프레임 추출 · 대조
- Git 전부

## 3.2 경로 A — 배치모드 (유니티가 **꺼져 있을 때**)

```bash
UNITY="/c/Program Files/Unity/Hub/Editor/6000.6.0f1/Editor/Unity.exe"
PROJ="C:\Users\SAMSUNG\OneDrive\Desktop\Spiderman_Ver2"

# 임의의 정적 메서드 실행
"$UNITY" -batchmode -nographics -projectPath "$PROJ" \
         -executeMethod BuildTools.Verify -logFile "$PROJ\tools\verify.log"

# EditMode 테스트
"$UNITY" -batchmode -projectPath "$PROJ" -runTests -testPlatform EditMode \
         -testResults "$PROJ\tools\testresults.xml" -logFile "$PROJ\tools\p_tests.log"
```
PowerShell 래퍼도 있다: `tools\unity.ps1 <Method>` · `tools\verify.ps1`

## 3.3 경로 B — ClaudeBridge (유니티가 **켜져 있을 때**) ★

유니티는 한 프로젝트를 두 인스턴스가 동시에 못 연다. 에디터가 켜져 있으면 배치모드가 막힌다.
그래서 파일 우편함을 만들었다. `Assets/Editor/ClaudeBridge.cs`

```
tools/claude_cmd.txt      Claude가 쓴다. 1줄=고유ID, 2줄=정적 메서드 이름
tools/claude_result.txt   브릿지가 결과를 쓴다 (status=OK/FAIL)
tools/claude_compile.txt  컴파일 에러를 쓴다  ★ 결과를 읽기 전에 항상 이걸 먼저 본다
tools/claude_tests.txt    테스트 결과 (status=PASS/FAIL, passed/failed, 실패 목록)
tools/claude_state.txt    브릿지 내부 상태
```

**중요한 함정 2개:**
- 유니티 에디터는 **창이 포커스를 받아야** 업데이트 루프가 돈다.
  명령을 넣고 형님이 유니티 창을 클릭해야 실행된다. 이건 피할 수 없다.
- **컴파일이 실패하면 유니티는 직전에 성공한 DLL로 테스트를 돌린다.**
  그래서 "옛날 결과"가 그대로 나온다. 반드시 `claude_compile.txt`를 먼저 확인한다.
  (2026-09-09에 이걸로 한 번 속았다.)

## 3.4 경로 C — 에디터 메뉴 (형님이 직접 누를 수 있게)

같은 기능을 메뉴로도 노출한다. 브릿지가 안 돌 때의 대안이다.
```
Spider ▸ 씬 ▸ 놀이터 만들기 (Playground)
Spider ▸ 씬 ▸ P2 웹 붙이기
Spider ▸ 씬 ▸ P2 전체 적용 + 테스트
Spider ▸ 설정 ▸ P2 튜닝값 적용
Spider ▸ 테스트 ▸ EditMode 전체 실행
Spider ▸ Claude Bridge ▸ 상태 보기
```
**새 기능을 만들 때마다 여기에 메뉴를 하나 더 추가한다.** 그게 "딸깍"의 정체다.

## 3.5 형님이 하는 것 (이것만)

- **Play 버튼을 누르고 직접 조작해보기**
- 조작감·움직임·카메라가 좋은지 나쁜지 **판단**
- 유니티 창 클릭 (브릿지 깨우기)
- 에셋(모델/사운드) 고르기
- 커밋/푸시 승인

## 3.6 절대 하지 않는 것

- `.unity` / `.prefab` / `.asset` YAML을 **텍스트로 직접 편집**하지 않는다. 파일이 깨진다.
  → 반드시 에디터 API를 통한다.
- 컴파일 결과를 확인하지 않고 다음 단계로 넘어가지 않는다.

---

# 4. 목표 — 레퍼런스 분석 (실제로 본 것만 적는다)

## 4.1 레퍼런스 A — 스윙과 카메라 (141프레임 확인)

| 시간 | 내용 |
|---|---|
| 0.0–2.5초 | 옥상(자갈 바닥, 물탱크). **양손이 동시에 올라가 웹 두 가닥이 좌우로 동시 발사.** |
| 2.5–5.0초 | 발사 직후 뒤집힌다. 지면이 화면 위로. 다리·몸통이 보인다. |
| 5.0–16초 | 고속 스윙. 화면이 20~90도 기울고 완전 역전 구간도 있다. |
| 16–24초 | **저고도 골목.** 도로 차선이 또렷하다. 건물 **벽면**에 걸고 좌우 팔이 번갈아 나온다. |
| 24–28초 | **3인칭 구간** (§4.3) |
| 28–37초 | 마천루 구역. 유리 타워를 올려다보며 통과. |
| 37–44초 | **건물 타기** (§4.4) |
| 44–47초 | 처음 옥상으로 복귀. 착지. |

관찰된 것:
- **거의 전부 1인칭.** 3인칭은 4초 남짓.
- **카메라 롤이 극단적이다.** 방향을 틀면 화면이 눕는다.
- **양손이 동시에 보이는 게 기본.** 팔뚝이 화면 모서리를 크게 차지한다.
- **고개를 숙이면 몸통과 다리가 보인다.**
- **줄은 아주 가늘다.** 대부분 화면 밖으로 빠진다.
- **저고도 골목 스윙이 지배적.** 멀리 옥상이 아니라 옆 건물 벽면에 건다.
- 광각이라 화면 가장자리가 눈에 띄게 휜다.

## 4.2 레퍼런스 B — 1인칭 몸과 전투 ★ 새로 추가

**그래픽 수준은 낮다.** 건물은 단색 박스, 텍스처 거의 없음, 만화풍 구름, 그림자 미미.
**우리 Playground 큐브 도시보다 아주 조금 위다.** 이건 좋은 소식이다 — 그래픽은 목표가 아니다.

인상적인 것은 전부 **몸과 전투**다:

```
1인칭 몸
  · 빨간 장갑 낀 양손·팔뚝이 화면을 크게 차지한다
  · 아래를 보면 파란 다리와 발이 보인다
  · 발차기하면 발이 화면을 가른다
  · 스윙 중 팔이 앵커 쪽으로 뻗는다

옥상 전투 (20~33초 · 3fps 60프레임 확인)
  · 웹을 적에게 쏜다
  · ★ 적이 팔을 벌린 채 화면 쪽으로 날아온다 — 끌어당기기
  · 눈앞에 온 적을 1인칭 주먹으로 친다 (빨간 장갑이 블러되며 지나간다)
  · 타격 시 검은 연기 퍼프
  · 적이 튕겨 날아간다
  · ★ 바닥에 흰 거미줄이 뿌려지고 적이 거기 묶인다
  · 불붙은 머리의 보스급 적이 따로 있다
  · 전투가 끝나면 곧바로 옥상에서 뛰어내려 스윙으로 이어진다

★★ 전투 구간 내내 모드 전환이 한 번도 없다.
   웹 발사 → 끌어오기 → 주먹 → 묶기 → 뛰어내리기 → 스윙이 하나의 흐름이다.
   이게 이 게임이 하려는 것의 증거다.
```

## 4.3 3인칭 구간 (레퍼런스 A)

캐릭터가 **화면 높이의 3~5%**다. 점이다. 카메라는 따라붙지 않고 멀리 고정된 채
캐릭터가 그 앞을 지나간다. 리플레이/관전 카메라에 가깝다.
도시 대비 내가 얼마나 작은지를 보여주는 연출이다.

**→ 3인칭 카메라는 둘이다.**
1. **플레이용 추적 카메라** — 자세가 읽혀야 한다
2. **연출용 원경 카메라** — 캐릭터가 작아야 의미가 있다

합치려 하면 어느 쪽도 안 된다.

## 4.4 건물 타기의 실제 순서 (레퍼런스 A)

```
위를 본다 (60~85도)
→ 타워 '벽면' 높은 곳에 가는 줄을 쏜다        ← 옥상이 아니라 벽면이다
→ 유리면이 화면을 가득 채울 만큼 끌려 붙는다
→ 손바닥이 유리에 닿는다 (짚기)
→ 반대 손이 더 높이 쏜다 → 반복 → 옥상 진입
```

## 4.5 속도감의 정체

영상의 속도감은 실제 m/s보다 **카메라**에서 나온다. 넷이다.
1. **근접** — 건물이 화면 가장자리를 스치듯 지나간다
2. **롤** — 방향을 틀 때 화면이 눕는다
3. **광각** — 가장자리 왜곡이 보일 정도
4. **시선** — 위를 보고 달린다

⚠️ **2번 롤은 어지럼증을 유발한다.** Ver.1에서 사용자가 직접 겪었다.
1인칭 롤 세기를 설정으로 뺀다 (0 / 약 / 강, 기본 "약").
**화면을 기울이는 대신 몸을 기울이는** 방식이 기본이다.
(Ver.1에서 화면 기울기 0도 · 몸 기울기 32도로 멀미를 없앤 실적이 있다.)

## 4.6 PS4 스파이더맨에서 가져올 것

레퍼런스 두 편에 없지만 "그 손맛"을 만드는 것들이다. **그래픽이 아니라 규칙이다.**

```
이동
  · 스윙이 끊기지 않는다. 놓는 순간 다음 줄이 이미 준비되어 있다
  · 줄을 놓고 공중에서 자세를 잡는 시간이 있다 (곡예). 궤적은 안 바뀐다
  · 벽·바닥·모서리 어디에 닿아도 멈추지 않고 흘러간다
  · 지면 가까이에서 속도가 살아난다 (참조물이 많아서)

전투
  · 한 대 치면 다음 대상으로 이어진다 — 소프트 락온이 다음 적을 잡아준다
  · 타격에 히트스톱이 있다 (아주 짧게 멈춘다). 이게 묵직함의 정체다
  · 적이 맞으면 물리적으로 밀려난다. 애니메이션만 바뀌는 게 아니다
  · 공중에 띄우고 이어서 친다
  · 환경을 쓴다 — 벽에 처박기, 난간 너머로 던지기
  · 웹으로 묶으면 적이 무력화되고, 벽에 붙는다

공통
  · 플레이어가 정확히 조준하지 않아도 게임이 의도를 읽는다  ★ 이게 핵심
  · 어떤 행동이든 다른 행동으로 취소·연결된다. 굳는 구간이 없다
```

## 4.7 그래픽 목표 — 명시적으로 낮게 잡는다

```
지금부터 P11까지 도시는 저폴리 · 단색 · 텍스처 거의 없음으로 간다.
레퍼런스 B가 정확히 그 수준이고, 그걸로 충분히 인상적이다.

도시 디테일은 나중에 Fab 등에서 에셋을 사서 올리면 되는 부분이다.
거기에 시간을 쓰는 것은 이 게임을 좋게 만들지 않는다.

★ "그래픽이 별로다"라는 판단으로 우선순위를 바꾸지 않는다.
   판단 순서는 §13을 따른다.
```

---

# 5. 헌법 — 네 가지

## 5.1 FREE WEB — 이 게임의 정체성

플레이어가 건물을 하나하나 정확히 조준하지 않아도 된다.
플레이어가 **카메라 + 방향 입력 + 웹 입력**으로 의도를 주면, 게임이
**실제 world surface · 적절한 anchor · 사용할 손 · primary/secondary 역할**을 고른다.

> "내가 건물을 찍는 게임"이 아니라
> **"내가 어디로 가고 싶은지 정하면 캐릭터가 웹을 알아서 잘 쓰는 게임."**

허공에 가짜 앵커를 만들지 않는다. **실제 월드 표면에만 건다.**

## 5.2 MOTION FIRST — 물리가 이동을 결정한다

```
Gameplay Physics → Traversal State → Base Animation
→ Procedural Pose → IK/Contact → Camera → Audio/VFX
```
애니메이션이 궤적을 지배하지 않는다. **연출 때문에 게임플레이 물리를 바꾸지 않는다.**

## 5.3 ONE WEB SYSTEM — 웹은 게임의 유일한 인터페이스

웹은 다음 넷에 연결된다:
```
Environment      건물 · 벽 · 바닥 · 난간
Object           간판 · 드럼통 · 차 · 잔해        (P9)
Enemy            적 몸통                          (P10)
Enemy Body Part  머리 · 좌우 팔 · 좌우 다리       (P10)
```
같은 웹 입력이 **대상이 무엇이냐에 따라** 다르게 동작한다:
```
표면      → Swing · Zip · Climb · Wall movement
오브젝트  → Pull(당겨오기) · Throw(던지기)
적        → Pull(끌어오기) · Yank(넘어뜨리기) · Bind(묶기) · Slam(내리꽂기)
적 부위   → Leg Trip(다리 걸어 넘어뜨리기) · Disarm(무기 뺏기)
```

## 5.4 NO MODES — 모드 전환을 만들지 않는다 ★ 이번에 추가된 헌법

```
전투 모드, 이동 모드, 조준 모드 같은 것을 만들지 않는다.
Tab으로 모드를 바꾸지 않는다. (Ver.1의 Tab 모드 체계는 폐기 사유였다)

스윙 중에 적이 조준 안에 들어오면 같은 버튼이 적에게 걸린다.
적을 치던 중에 시선을 건물로 돌리면 같은 버튼이 스윙이 된다.
그 사이에 상태 전환 연출도, 입력 잠금도, 카메라 전환도 없다.

구현 규칙:
  · 웹 입력은 언제나 하나다. "무엇을 할지"는 대상이 정한다.
  · 근접 공격은 별도 모드가 아니라, 적이 사거리 안에 있을 때의 같은 입력이다.
  · 어떤 동작도 다른 동작으로 취소·연결될 수 있어야 한다.
  · 굳는(입력이 안 먹는) 구간을 만들지 않는다. 히트스톱은 예외지만 100ms 이하다.

★ 새 기능을 넣기 전에 반드시 묻는다:
  "이게 모드를 만드는가? 만든다면 다시 설계한다."
```

---

# 6. 듀얼 트랙 — Three.js는 실험장, Unity는 본선

Ver.1 동결을 해제했다. 다만 "다시 Three.js로 간다"가 아니다. 역할을 나눈다.

## 6.1 왜

1. **유니티의 이득은 P6부터 나온다.** 애니메이션·리그·IK·래그돌·카메라가 유니티를 고른 이유인데,
   P1~P5는 캡슐로 한다. 그 구간엔 이득 0, 마찰 100%다.
2. **Claude의 검증 능력이 유니티에서 크게 떨어진다.** Play를 못 누르고, 형님이 창을 클릭해야 한다.
   Three.js에서는 Claude가 브라우저를 직접 몰고 스크린샷을 찍는다. **2026-09-09 실측 확인.**
3. 그럼에도 엔진을 갈지 않는 이유: **Ver.1이 좋았던 건 알고리즘이지 엔진이 아니다.**
   그리고 레퍼런스 B가 요구하는 건 전부 애니메이션·IK·래그돌 — Three.js의 최약점이다.

**자동 검증이 되는 구간은 Three.js가 빠르고, 형님이 눈으로 봐야만 하는 구간은 Unity가 압도적이다.**
이 분업이 듀얼 트랙의 근거다.

## 6.2 역할

```
Ver.1 (Three.js)   감각 실험장. "이 느낌이 맞는가"를 여기서 먼저 답한다.
                   Claude가 직접 실행·조작·측정·스크린샷 한다.
Ver.2 (Unity)      본선. 실험장에서 확정된 것만 들어온다.
```

## 6.3 규칙

1. **Ver.1에 새 기능을 만들지 않는다.** 결과물은 "숫자" 또는 "알고리즘"이지 "기능"이 아니다.
2. **`game3d.js`에 코드를 더하지 않는다.** 반드시 `src/` 모듈로 뺀다.
   그걸 못 하는 실험이면 Ver.1에서 하지 않는다. God File 벽을 다시 쌓지 않기 위해서다.
3. **확정된 숫자는 이 문서 §8에 먼저 적고, 그다음 `TuningConfig`에 넣는다.**
4. **Unity에서 "느낌이 이상하다"가 나오면 Ver.1에서 같은 상황을 재현한다.**
   Ver.1에서도 이상하면 알고리즘 문제, 멀쩡하면 이식 문제다. 이 구분이 존재 이유다.
5. **`v1.0-final`(fe4a5cb)은 건드리지 않는다.** 실험은 별도 브랜치.
6. **P6 진입 시 실험장을 닫는다.** 그 뒤로는 두 살림 비용이 이득을 넘는다.

## 6.4 실행 방법

```bash
cd "C:\Users\SAMSUNG\OneDrive\Desktop\spider man" && node serve.js
# http://localhost:8173/game3d.html → 버튼 "자유 이동"
# F1 도움말 · F3 앵커 디버그 · F4 앵커 V2/legacy 전환
# serve.js에 POST /__shot 이 있어 페이지가 스스로 화면을 shots/ 에 저장한다
```

---

# 7. Ver.1에서 무엇을 가져오는가

Ver.1은 폐기물이 아니라 **연구 프로토타입**이었다. 배운 것:

1. 좋은 모델을 넣는다고 게임이 좋아지지 않는다.
2. 3인칭 액션에서 Animation / Pose / IK / Camera가 매우 중요하다.
3. 웹 물리 자체보다 **Auto Anchor와 캐릭터 표현의 완성도**가 체감을 좌우한다.
4. 이동용 웹과 공격용 웹이 분리되면 "자유로운 웹 사용"이 약해진다. → 헌법 §5.4
5. **God File**로 계속 추가하면 유지보수가 급격히 어려워진다.
   (game3d.js 8,076줄 · 최상위 `let` 192개 · 함수 209개가 그걸 전부 공유)
6. 전체를 동시에 키우는 것보다 **핵심 Vertical Slice를 먼저** 완성해야 한다.

## 7.1 [A] PORT — 알고리즘을 C#으로 옮긴다 ★

전부 순수 함수다. THREE를 쓰지 않는다. 이식이 기계적이다.

| Ver.1 파일 | 줄수 | 내용 | 상태 |
|---|---|---|---|
| `src/anchor.js` | 161 | **자동 앵커 V2** — 의도 벡터 · 점수식 7요소 · 손 선택 | **P3에서 이식** |
| `src/wallplant.js` | 96 | 벽 짚기 — 접촉 예측 · 속도 보존 임펄스 | P5 |
| `src/vclimb.js` | 64 | 웹 수직 이동 — 앵커 선정 · 발사 조건 | P5 |
| `src/reach.js` | 155 | 손이 향할 곳 — 각도 제한 · tanh 부드러움 | P6 |
| `src/mathx.js` | 81 | 보조 수학 | 필요할 때 |
| `src/fp-hands.js` | 261 | 1인칭 손/팔 배치 — **알고리즘만** 참고 | P6 |

## 7.2 [A2] 이미 이식 완료 (커밋 462e314)

`game3d.js:4151-4241 · 6134-6222 · 6244-6303` → `WebPhysics.cs` · `MotionMath.cs` · `PlayerMotor.cs`

```
GRIP_TIME 0.13        붙는 순간 덜컹거림 제거. 서서히 문다
자동 펌핑 PUMP_DEPTH  호 바닥에서 줄이 저절로 감긴다 ★ "조작감"의 정체였다
base/len 2단 로프     목표 길이와 실제 길이 분리, 초당 40m로 따라간다
로프 구속             반경 제거 후 SWING_CONVERT×grip 만큼 되돌림
                      + 위치를 먼저 예측하고 그 자리에서 푼다
지수형 소프트캡       exp(-초과율² × 3 × dt) ★ "너무 빠르다"의 답이었다
드래그 3단            지상 0.06 / 공중 0.02 / 스윙 0.003
지상 이동             목표 속도 수렴 방식 (가속을 계속 더하지 않는다)
중력 72 · 점프 42     Ver.1 G, JUMP_V
```

## 7.3 [B] REDESIGN — 아이디어만

미션 / 보스 / 세이브 / 적 AI / 튜토리얼 / HUD.
돌아가지만 God File에 얽혀 있어 코드를 옮길 가치가 없다. 설계만 참고한다.

## 7.4 [C] DISCARD

- `game3d.js` 8,076줄 통짜 파일
- **Tab 모드 전환 체계** — 헌법 §5.4 위반
- 1인칭 팔을 원통으로 조립한 것 (유니티는 진짜 모델을 쓴다)
- **난수 제약** — Ver.1에서는 THREE 생성자가 `Math.random()`을 먹어 도시 생성 난수열이 밀렸다.
  Ver.2는 전용 PRNG(`SpiderVer2.Core.Rng`)를 쓴다. 이미 구현·테스트 완료.

## 7.5 [D] ASSET

- Mixamo 클립 14종(`.glb`) → 유니티 Humanoid로 바로 들어간다.
  위치: `Ver.1/assets/models/player/anims/`
- 도시/텍스처 → 버린다.

---

# 8. 실측 숫자표 ★ 가장 중요한 인수인계물

Ver.1에서 **브라우저 실측으로 맞춘** 값들이다. 유니티에서 처음부터 다시 맞추려면 몇 주가 걸린다.
전부 `Assets/Settings/TuningConfig.asset`에 들어가 있다. Inspector에서 즉시 만질 수 있다.

## 8.1 자동 앵커 (src/anchor.js — P3에서 사용)

```
SP_REF       55      '빠름'의 기준 속도 (m/s)
W_CAM_SLOW   1.60    느릴 때 카메라 비중
W_CAM_FAST   0.70    빠를 때 카메라 비중 (낮춰야 90도 급snap이 안 난다)
W_MOM_SLOW   0.25    느릴 때 관성 비중
W_MOM_FAST   1.55    빠를 때 관성 비중
W_MOVE       0.55    WASD 방향
W_TURN       0.90    A/D만의 옆 편향

W_FWD        1.60    의도 방향 일치도
W_LEN        1.20    줄 길이의 질
W_STEEP      0.80    머리 바로 위 회피
W_HIGH       1.10    높이
W_HAND       0.55    손 쪽 편향 (기계적 교대가 안 될 만큼만)
W_ALT        0.30    직전 손의 반대쪽 보너스
W_SHARP      1.30    급선회 벌점 (속도 비례)

LEN_BEST     0.62    최대 사거리의 62%가 최적 호
LEN_SPAN     0.45
DROP_MAX     14      후보 선정에서 이보다 아래는 그네가 아니라 추락
                     ※ 플레이어가 직접 조준한 곳은 막지 않는다 (헌법 §5.1)
BACK_MIN    -0.15    의도 기준 이보다 뒤면 기각

부채꼴 yaw   0, ±15, ±32, ±52, ±74, ±96  (손 쪽으로 최대 10도 편향)
부채꼴 pitch 34, 44, 24, 56, 14, 68, 4, -6
총 88발 · 0.156ms/회 (Ver.1 기준)
```

## 8.2 로프 · 속도

```
ROPE_MIN        12      최소 로프 길이
SWING_MIN_LEN   18      이보다 짧으면 그네가 아니라 벽에 박는다
ROPE_MAX        150     사거리 = 최대 로프 길이
SWING_CONVERT   0.995   낙하(반경) 속도를 접선으로 되돌리는 비율
MAX_SPEED       112
SOFT_SPEED      82      이 위로는 하드 클램프 대신 지수 드래그
ACCEL           118     지상 가속
AIR_ACCEL       42      공중 조작 (웹 없으면 ×0.6)
PUMP_ACCEL      58      펌프(E) 속도 증강
DT              1/120   고정 타임스텝
```

## 8.3 웹 조작 (Ver.1 game3d.js:4237-4241)

```
GRIP_TIME          0.13   로프가 완전히 물리기까지. 붙는 순간 덜컹거림 제거
ROPE_FOLLOW_RATE   40     실제 길이가 목표를 따라가는 속도 (m/s)
REEL_MANUAL        26     Space 홀드 시 줄 감기 속도 (m/s)
PUMP_DEPTH         0.12   자동 펌핑 강도. 호 바닥에서 로프가 줄어드는 비율
REEL_GAIN_MAX      1.01   감기 한 스텝 최대 배율 (없으면 앵커 근처에서 발산)
ZIP_PULL           55     감을 때 앵커 쪽으로 끌려가는 가속 — Ver.2 신규
ROPE_STIFF         1.0    위치 보정 강도 (Ver.1은 즉시 스냅)
DRAG_GROUND        0.06
DRAG_AIR           0.02
DRAG_SWING         0.003  ★ 여기서 깎으면 속도가 절대 안 쌓인다
```

## 8.4 지상 이동

```
GRAVITY       72     Ver.1 G. 큰 스케일에서 붕 뜨지 않게 실제 중력보다 크게
JUMP_SPEED    42     Ver.1 JUMP_V. 중력을 올린 만큼 같이 올렸다
MOVE_SPEED    19     걷기 최고 속도
SPRINT_MULT   1.85   지상 Shift 달리기 배수
GROUND_CHECK  0.25   접지 판정 거리
```

## 8.5 벽 짚기 (src/wallplant.js — P5)

```
PLANT_LOOK      0.20   이 시간 안에 벽면을 넘어설 것 같으면 짚는다
PLANT_MIN_V     15     이보다 느리면 짚지 않는다 (저속은 '벽 붙기'의 몫)
PLANT_TIME      0.30   손이 벽에 닿아 있는 시간
PLANT_KEEP      0.88   벽면을 따라 흐르는 속도 유지율
PLANT_BOUNCE    0.45   벽을 향하던 속도의 이만큼으로 되밀린다
PLANT_PUSH      11     최소 밀기
PLANT_PUSH_MAX  28     상한 (없으면 고속에서 튕겨 날아간다)
PLANT_UP        7      살짝 위로 (없으면 짚을 때마다 고도가 깎인다)
PLANT_CD        0.45   되짚기 방지
PLANT_HAND_Y    1.15   접촉점을 어깨 높이로
```

**절대 조건: 벽 짚기에서 속도를 0으로 만들지 않는다.**
고정값 15로 밀었더니 46 m/s로 접근한 것이 15.9 m/s로 나갔다. 속도 비례 반발로 바꿔야 한다.

## 8.6 웹 수직 이동 (src/vclimb.js — P5)

```
VC_NEAR    9      벽에서 이 안쪽이어야 '건물을 타는 중'
VC_STEP    45     한 번에 타고 오르는 높이
VC_OUT     1.5    벽에서 앵커까지 이격 (Ver.1은 3.6, §4.4 기준으로 낮췄다)
VC_ARRIVE  7      앵커에 이만큼 가까워지면 다음 자리
VC_TOP     2.5    지붕을 이만큼 넘겨 잡아야 옥상으로 올라선다
VC_CD      0.30   연속 발사 간격
```

## 8.7 보조 웹 · 슬링샷 · 집라인 (P4)

```
WEB2_PULL    62     보조 웹이 당기는 가속 (m/s²)
WEB2_FADE    45     이 거리부터 약해진다
SLING_MAX    0.50   최대 충전 시간
SLING_MIN    0.10
SLING_BOOST  64     최대 충전에서 더해지는 속도
SLING_REEL   0.22   밀고 나가면서 줄을 감는 비율
SLING_DRAG   5.2    물고 있는 동안 속도 감쇠
ZIP_SPEED    150    집라인 목표 속도
ZIP_GRAB     11     목표 속도에 붙는 빠르기
ZIP_ARRIVE   10     앵커에 이만큼 가까워지면 종료
ZIP_KEEP     0.5    도착 시 남기는 속도 비율
ZIP_MOMENT   0.5    상한이 MAX_SPEED로 돌아오기까지의 유예(초)
```

## 8.8 카메라

```
CAM_ROLL           0.5    3인칭 뱅킹 롤 강도
SOFT_CONE          0.10   소프트 락온 원뿔 (대략 ±84도)
M_BUF_T            0.42   선입력 유지 시간
FP_FOV             78     1인칭 기본 시야각 (광각)
FP_FOV_SPEED_ADD   14     최고 속도에서 더해지는 시야각
LOOK_SENS          0.12   마우스 감도 (도/픽셀)
PITCH_LIMIT        89
FP_ROLL            0.35   1인칭 롤 세기 (0=끔 / 0.35=약 / 0.5=강)
FP_LEAN_BODY       true   화면 대신 몸을 기울인다 ★ 멀미 대책
FP_LEAN_MAX        32     몸 기울기 최대 각도
```

## 8.9 실측 성능 기준 (Ver.1)

```
자동 앵커 V2 vs legacy — 조준 없이 연속 스윙
  V2      6스윙   376m / 6.3초   평균 85 m/s   손 L R L L L L
  legacy 14스윙   157m / 13.6초  평균 33 m/s   손 R R R R R R ...
→ 2.4배 멀리, 절반 시간에, 2.6배 속도. Ver.2는 이 이상을 목표로 한다.
```

---

# PART A. 인게임 플레이

> 플레이어가 손으로 만지는 모든 것. 이 게임이 재미있는지는 전부 여기서 결정된다.

## A1. 이동

```
지상    걷기 19 m/s · Shift 달리기 1.85배 · 점프 42
        목표 속도 수렴 방식 (가속을 계속 더하지 않는다)
공중    AIR_ACCEL 42 (웹 있을 때) / ×0.6 (웹 없을 때)
        중력 72. 붕 뜨지 않는다
상한    SOFT_SPEED 82 위로 지수 드래그, MAX_SPEED 112 하드캡
        ※ 집라인·다이브는 이 상한을 의도적으로 넘는 예외다
벽      벽 붙기(저속·의도적) / 벽 짚기(고속·자동) — 완전히 다른 상태다
```

## A2. 웹 — 이동

```
발사     마우스 좌(오른손) / 우(왼손). 누르고 있는 동안 붙어 있다
앵커     실제 월드 표면에만. 허공 금지
         P3부터: 조준 없이 의도 벡터 기반 자동 선정 (부채꼴 88발)
로프     base(목표) / len(실제) 2단. len이 초당 40m로 base를 따라간다
구속     반경 속도 제거 + SWING_CONVERT×grip 만큼 접선으로 되돌림
         grip은 GRIP_TIME 0.13초에 걸쳐 0→1
자동펌핑 호 바닥에 가까울수록 로프가 12%까지 자동으로 감긴다
         ★ 아무 버튼도 안 눌러도 스윙이 유지된다
수동감기 Space 홀드. 각운동량 보존으로 가속 + 앵커 쪽으로 끌림
펌프     E 홀드. 수평 진행 방향으로 가속
양손     P4부터. Primary = 실제 구속, Secondary = 조향력
         ※ true dual hard constraint는 하지 않는다. 두 줄이 서로 잡아당겨 진동한다
전환     한 손이 붙은 채 다른 손을 쏘면 자연스럽게 넘어간다
```

## A3. 웹 — 벽과 건물 (P5)

```
벽 짚기   고속으로 벽에 닿을 때 손을 짚고 계속 간다. 속도를 0으로 만들지 않는다
벽 타기   벽면 높은 곳에 걸고 끌려 올라간다. 손이 번갈아 나간다
          화면에서 웹이 상승의 원인처럼 보여야 한다
옥상 진입 지붕을 VC_TOP(2.5m) 넘겨 잡아야 올라선다
벽 붙기   저속·의도적. 벽에 붙어 기어다닌다
```

## A4. 웹 — 오브젝트 (P9)

```
당기기   간판·드럼통·잔해에 걸고 당겨온다. 무게에 따라 내가 끌려갈 수도 있다
던지기   들고 있는 것을 조준 방향으로 던진다
         ★ 스윙 중에도 된다. 멈추지 않는다
```

## A5. 웹 — 전투 (P10) ★ 모드 없음

```
같은 웹 입력이 대상에 따라:
  적 몸통에 걸고 당기기      적이 팔 벌리고 날아온다 (레퍼런스 B 확인)
  적 다리에 걸고 당기기      넘어뜨린다
  적에게 걸고 내가 당겨가기  거리를 좁힌다
  묶기                       적이 벽/바닥에 붙는다. 흰 거미줄이 뿌려진다
  공중 적을 끌어내리기 / 지상 적을 띄우기

근접 격투 (별도 모드가 아니다)
  적이 사거리 안에 있을 때의 같은 입력이 주먹·발차기가 된다
  1인칭에서 손·발이 화면을 가른다
  히트스톱 ≤ 100ms. 이게 묵직함을 만든다
  적이 물리적으로 밀려난다. 애니메이션만 바뀌는 게 아니다
  소프트 락온이 다음 적을 잡아준다 (SOFT_CONE 0.10)
  던지기 / 벽에 처박기 / 난간 너머로 넘기기
  래그돌로 꺾이며 떨어진다

연결 규칙
  전투 중 언제든 시선을 건물로 돌리고 웹을 쏘면 그대로 스윙이 된다
  스윙 중 언제든 적이 사거리에 들어오면 그대로 전투가 된다
  전환 연출도, 입력 잠금도, 카메라 전환도 없다
```

## A6. 카메라

```
1인칭 (기본 · 레퍼런스 A/B 둘 다 1인칭이다)
  FOV 78 + 속도 비례 14도 추가
  몸 기울이기 기본 (화면 롤은 설정 0/약/강)
  고개를 숙이면 가슴·다리·발이 보인다
  스윙 중 팔이 앵커를 향한다

3인칭 (P7)
  추적 카메라 — Cinemachine. 자세가 읽히는 크기 유지
  연출용 원경 카메라 — 캐릭터가 화면의 3~5%. 도시 대비 크기를 보여준다
  둘은 별개 시스템이다. 합치지 않는다

공통
  카메라가 게임플레이를 납치하지 않는다
  1인칭 ↔ 3인칭 전환 시 논리 상태가 유지된다
```

## A7. 1인칭 몸 (P6) ★ 레퍼런스 B의 핵심

```
양손·팔뚝이 화면 모서리를 크게 차지한다
아래를 보면 가슴·다리·발이 보인다
웹은 실제 손 본에서 나간다 (몸통 근사 금지)
손이 앵커 방향을 향한다 (Two Bone IK)
손별 상태기: IDLE → SHOOT → CATCH → HOLD → RELEASE
스윙 페이즈: ATTACH / DESCEND / BOTTOM / ASCEND / RELEASE / FREE_AIR
절차적 포즈는 Animator가 클립을 적용한 뒤, 렌더 전에 얹는다
  ★ 순서를 틀리면 다음 프레임에 사라진다 (Ver.1에서 겪음)
```

## A8. 조작키 (현재 · Ver.1 배치)

**새 조작을 추가하면 F1 인게임 도움말에도 반드시 같이 넣는다 (작업 규칙 6).**

```
W A S D              이동
마우스               시점
Space                점프 (땅 + 웹 없을 때) / 줄 감기 (매달린 중)
E                    펌프 (매달린 중)
왼쪽 Shift           달리기 (땅)
마우스 왼쪽          오른손 웹 — 누르고 있는 동안 유지
마우스 오른쪽        왼손 웹
Esc                  마우스 커서 풀기
F1                   도움말 (전체화면 · 게임 일시정지 · 입력 차단)
F3                   디버그 오버레이
```

---

# PART B. 플레이 외의 것들

> 플레이어가 직접 만지지 않지만, 없으면 게임이 성립하지 않는 것들.

## B1. 월드 · 도시

```
현재     Playground.unity — 코드 생성. 시드 고정 PRNG로 재현 가능
         시작 옥상(물탱크) · 넓은 거리(12동) · 좁은 골목(폭 10m, 14동)
         높은 벽 90m · 높이 다른 플랫폼 9개 · 차선 무늬 바닥 · 안개

원칙     저폴리 · 단색 · 텍스처 거의 없음 (§4.7)
         도시 디테일은 P11 이후, Fab 에셋으로 올린다
         ★ 그래픽에 시간을 쓰는 것은 이 게임을 좋게 만들지 않는다

필수 조건 (게임플레이가 요구하는 것)
  · 벽면에 걸 곳이 충분히 많을 것 (레퍼런스는 옥상이 아니라 벽면에 건다)
  · 저고도 골목이 있을 것 (속도감의 원천 §4.5)
  · 높이가 다양할 것
  · 옥상이 평평하고 넓을 것 (전투 무대 — 레퍼런스 B)
  · 지면에 참조물(차선 등)이 있을 것

생성     SceneBuilder.cs가 코드로 만든다. 손으로 배치하지 않는다
```

## B2. 적 (P10)

```
아키타입  Agile(빠름) · Brute(무거움) · Tech(원거리) · Boss
          ※ 특정 상업 IP 이름을 코드에 박지 않는다 (§B8)
부위      Chest · Head · LeftArm · RightArm · LeftLeg · RightLeg
          본이 있으면 본, 없으면 virtual offset + 캡슐 판정
상태      IDLE / ALERT / APPROACH / ATTACK / STAGGER / PULLED / BOUND / RAGDOLL
반응      웹에 맞으면 물리적으로 끌려온다
          맞으면 밀려난다. 애니메이션만 바뀌는 게 아니다
          묶이면 무력화되고 벽/바닥에 붙는다
          죽거나 떨어지면 래그돌
AI        먼저 1~3명으로 시작한다. 군집 AI는 P11
```

## B3. 미션 · 진행 (P11)

```
Ver.1의 미션/보스 설계를 참고하되 코드는 옮기지 않는다.
Mission State를 Player 스크립트에 박지 않는다 (미래 멀티플레이 대비).
```

## B4. UI · HUD · 도움말

```
현재     DebugOverlay.cs — IMGUI
         1080 기준 가상 해상도로 그린 뒤 화면 크기에 맞춰 확대
         ★ 이걸 안 하면 QHD에서 조준점이 4픽셀로 쪼그라든다 (실제로 겪음)

조준점   중앙 십자. 검은 테두리(밝은 하늘 대비) + 가운데 빈 공간
         웹이 붙으면 초록으로 바뀐다

디버그   Speed · Velocity · Position · State · Grounded · Pitch/Lean
         Web 상태 · Rope len · Anchor · Age · REELING
         P3부터: Candidate Count · Selected Score · Intent Direction · 기각 사유
         F3 토글. 릴리즈 기본 OFF

도움말   F1. 전체화면 · 게임 일시정지(timeScale 0) · 입력 완전 차단
         조작법 전부 + "속도를 버는 법"
         ★ 새 조작을 추가하면 여기도 같이 고친다 (작업 규칙 6)

정식 HUD  P11. 체력 · 미션 · 적 표시
```

## B5. 오디오 (P11)

```
Ver.1에 audio.js 326줄이 있다. 설계만 참고한다.
웹 발사(thwip) · 타격 · 바람 · 착지가 우선순위다.
```

## B6. 저장 (P11)

```
Character State와 Mission State를 분리한다.
Player 스크립트에 박지 않는다.
```

## B7. 빌드 · 배포 (P12)

```
주 타깃   Windows 데스크톱
WebGL     P12에서 검토. 저폴리라 가능성은 있으나 빌드 크기 검증 필요
          Ver.1의 "링크만 주면 친구가 바로 함"은 재현되지 않을 수 있다
```

## B8. IP · 에셋 원칙

```
개발 구조를 특정 상업 IP에 하드코딩하지 않는다.
내부적으로 Spider / Agile / Brute / Tech 같은 archetype을 쓴다.
외부 에셋은 라이선스를 확인한다.
공개 배포 가능성이 생기면 저작권 있는 캐릭터/모델 사용 여부를 다시 검토한다.
```

## B9. 테스트 · 검증

```
순수 C# 로직 테스트    앵커 점수식 · 의도 벡터 · 로프 수학 · 벽 짚기 임펄스 · 속도 상한
                       → Claude가 직접 돌린다. 이게 Claude의 유일한 자립 검증 수단이다
EditMode 테스트        씬 없이 도는 것 전부. 현재 37개
PlayMode 테스트        필요할 때만
런타임 디버그 오버레이 Free Web은 이게 없으면 튜닝이 불가능하다

★ 설계 원칙: 감각을 결정하는 수학은 반드시 MonoBehaviour 밖 순수 함수로 뺀다.
  MonoBehaviour 안에 묻히면 Claude가 검증할 수 없고, 형님 왕복이 늘어난다.
  (2026-09-09에 속도 상한을 PlayerMotor에서 MotionMath로 뺀 것이 그 예다)
```

### 수동 플레이 테스트 시나리오 (각 단계마다)

| | 상황 | 확인 |
|---|---|---|
| A | 넓은 거리 고속 직진 | 속도 유지 |
| B | 고속 → A → 웹 | 왼쪽 실제 앵커 · 과도한 snap 없음 |
| C | 고속 → D → 웹 | 오른쪽 |
| D | 교대 웹 R → L → R | 손이 실제로 갈리는가 |
| E | 지면 근처 저고도 | 엉뚱한 ground anchor · 충돌 |
| F | 고층 스카이라인 | |
| G | 고속 벽 접근 | 멈추지 않고 짚는가 |
| H | 벽 → 높은 앵커 → 옥상 | 웹이 상승의 원인처럼 보이는가 |
| I | 1인칭 ↔ 3인칭 전환 | 논리 상태 유지 |
| J | 스윙 → 적 → 웹 → 타격 → 다시 스윙 | **모드 전환 없이 이어지는가** |

## B10. 성능

```
매 프레임 도시 전체에 full raycast 하지 않는다.
  Broad Phase → Cheap Score → Expensive Validation
스크래치 Vector3/Quaternion 재사용 · 본 참조 캐시 · 이펙트 풀링
Profiler 근거 없이 최적화하지 않는다
초기부터 WebGL 최적화에 매몰되지 않는다
```

## B11. 처음부터 만들지 않을 것

AAA 도시 · HDRP · 수십 캐릭터 · 멀티플레이 · MMO · 대규모 NPC 시뮬레이션 ·
거대 스킬트리 · 20시간 스토리 · 배틀패스 · 상점 · 인벤토리/제작 · DOTS/ECS ·
복잡한 Addressables · 프로토타입 전 true dual constraint · 완전 절차적 바디 애니메이션 ·
거대 커스텀 물리 엔진.

**멀티플레이는 현재 범위가 아니다.** 다만 미래 2~4인 PvE 가능성을 위해:
Player를 전역 싱글턴에 과도하게 결합하지 않고, Character State를 분리하고,
Mission State를 Player 스크립트에 박지 않고, Combat action을 명확한 event/state로 표현한다.
**단, 이 준비 때문에 현재 개발이 느려지면 안 된다.**

---

# 9. 개발 단계 P0 → P12

각 단계는 **완료 기준을 만족해야** 다음으로 넘어간다.
한 커밋에 리팩터 + 새 기능 + 튜닝 + 에셋 교체를 섞지 않는다.

## ✅ P0 — 프로젝트 부트스트랩 (완료 · 30073d0)

Unity 6.6 URP 프로젝트 CLI 생성 · 패키지 5종 · 폴더 구조 13개 ·
`BuildTools.Verify` · `tools/unity.ps1` · Git + 원격.
**완료 기준 충족:** `BuildTools.Verify` 종료코드 0.

## ✅ P1 — 놀이터 + 튜닝 설정 (완료 · fb66e6e)

`SceneBuilder`로 Playground 씬 코드 생성 · 캡슐 플레이어 + Rigidbody ·
Input System 액션맵(코드 구성) · 1인칭 카메라 · `TuningConfig` · 디버그 오버레이 · 전용 PRNG.
**완료 기준 충족:** 캡슐이 움직이고 점프하고 벽에 부딪힌다.

## 🔶 P2 — 단일 웹 물리 (코드 완료 · 640e040 + 462e314 · **검증 대기**)

- [x] 실제 월드 표면 레이캐스트 앵커
- [x] 로프 구속 (반경 제거 + 접선 보존 + GRIP)
- [x] 가는 LineRenderer (0.035m)
- [x] 붙이기 / 놓기 / 감기 / 펌프 / 자동 펌핑
- [x] 고정 타임스텝 1/120 + Rigidbody Interpolate
- [x] EditMode 테스트 37개
- [x] Ver.1 스윙 물리 전면 이식 (§7.2)
- [ ] **462e314 이후 컴파일·테스트 미확인** ← 새 세션의 첫 작업
- [ ] **형님 감각 판정 미완**

**완료 기준: 캡슐 상태로도 웹스윙이 재미있다.**
> 이게 아니면 그 뒤가 전부 의미 없다. 여기서 반드시 멈추고 형님 판정을 받는다.

## P3 — AUTO ANCHOR V2 ★ 이 게임의 정체성

- [ ] **먼저 디버그 시각화부터 만든다.**
      후보 앵커 구슬(초록 통과 / 빨강 기각 / 노랑 채택) · 점수 · 기각 사유 · 의도 벡터 · 속도 벡터
- [ ] `anchor.js` → `WebAnchorResolver.cs` 이식 (§8.1 숫자 그대로)
- [ ] 의도 벡터 = 카메라 + 관성 + WASD + 선회
- [ ] 부채꼴 ±96도 · 낮은 피치 포함 · 화면 밖 앵커 허용
- [ ] 손 선택 (레이는 한 번, 후보마다 좌/우 두 점수)
- [ ] 성능: broad phase → cheap score → 최종 몇 개만 정밀 검증
- [ ] **웹 타깃 인터페이스를 여기서 설계한다** — Surface / Object / Enemy / EnemyPart
      P9·P10에서 같은 시스템에 대상만 추가되도록 (헌법 §5.3, §5.4)

> **왜 시각화가 먼저인가:** Ver.1에서 이걸 만들자마자 "좌우 점수가 소수점 셋째 자리까지 같다
> (4.114 / 4.114)"는 걸 첫 화면에서 발견했다. 없었으면 추측으로 고쳤을 것이다.

**완료 기준:** 정확히 조준하지 않고 A/D만으로 좌회전·우회전·직진이 의도대로 나온다.

## P4 — 좌/우 웹 · 보조 웹 · 집라인

- [ ] 손별 독립 논리 상태 (`Left`/`Right`: connected · anchor · role · length · age)
- [ ] 손 선택 규칙 (한 손만 비면 그 손 / 둘 다 비면 target side / 교대 고려)
- [ ] Primary = 실제 로프 구속, **Secondary = 조향력** (true dual constraint 아님)
- [ ] 전환(transfer) · 릴리즈 · 양손 동시 발사 (§4.1 시작 장면)
- [ ] 집라인 (§8.7) — 속도 상한 예외 처리 포함
- [ ] 테스트: 속도 폭발 없음 · 두 앵커 사이 jitter 없음 · single-web fallback

**완료 기준:** 고속에서 좌우 방향 전환이 자유롭고, 손이 실제로 교대한다.

## P5 — 벽 짚기 · 웹 수직 이동 · 벽 붙기

- [ ] `wallplant.js` → `WallPlantController.cs` (§8.5)
- [ ] `vclimb.js` → `VerticalClimb.cs` (§8.6)
- [ ] **Wall Plant와 Wall Cling은 완전히 다른 상태다.** 고속 = Plant, 저속/의도적 = Cling
- [ ] 옥상 진입 전환
- [ ] 테스트: 고속 접촉에서 속도 0 금지 · 벽 관통 금지

**완료 기준:** 고속으로 벽에 닿을 때 멈추지 않고 손을 짚고 계속 간다.
웹으로 건물을 올라가고, **화면에서 웹이 상승의 원인처럼 보인다.**

## P6 — 휴머노이드 · 1인칭 몸 ★ 레퍼런스 B의 핵심

> **여기서 Ver.1 실험장을 닫는다 (§6.3-6).**
> P1~P5는 캡슐로 한다. 모델 교체와 이동 리팩터를 동시에 하면 버그 원인을 구분할 수 없다.

- [ ] 모델 임포트 · Humanoid Avatar 설정
- [ ] Animator + Mixamo 기본 클립 (Ver.1 `assets/models/player/anims/`)
- [ ] Animation Rigging 패키지
- [ ] **실제 손 본에서 웹이 나간다** (몸통 근사 금지)
- [ ] `reach.js` → 손/팔이 앵커 방향을 향한다 (Two Bone IK)
- [ ] 손별 상태기 · 스윙 페이즈
- [ ] **1인칭 몸 — 고개를 숙이면 가슴·다리·발이 보인다** (§4.2 · A7)

**완료 기준:** 1인칭에서 팔이 실제로 웹을 잡는 것처럼 보이고, 아래를 보면 몸이 있다.

## P7 — 카메라 (둘로 나눈다)

- [ ] **추적 카메라** (Cinemachine): 거리 곡선 · 속도 look-ahead · FOV · 충돌 · 스윙 구도
- [ ] **연출용 원경 카메라**: 캐릭터가 작게 보이는 관전 컷 (§4.3)
- [ ] 1인칭 롤 설정 (0/약/강) + 몸 기울이기
- [ ] 광각 + 근접감 (§4.5)
- [ ] 카메라가 게임플레이를 납치하지 않는다

**완료 기준:** 레퍼런스 영상과 나란히 놓고 비교 가능하다.

## P8 — 레퍼런스 A 재현 (Vertical Slice 1)

```
옥상 점프 → 자동 오른손 웹 → 캐치 → 스윙 → 왼손 보조 웹 → 급선회
→ 전환 → 릴리즈 → 자동 곡예 → 벽 짚기 → 높은 웹 → 릴 → 벽 밀기 → 옥상 진입
```
- [ ] 자동 곡예 (**물리 궤적을 바꾸지 않는다** — Ver.1에서 이걸 어겨 속도가 몰래 붙었다)
- [ ] 녹화해서 레퍼런스 A와 항목별 비교
      (캐릭터 화면 크기 · 웹 원점 · 어느 손 · 캐치 · 몸 기울기 · 포즈 변화 ·
       화면 밖 웹 · 선회 예리함 · 모멘텀 유지 · 벽 접촉 · 수직 이동 · 릴리즈 · 카메라 구도)

**⛔ 이 단계에 도달하기 전에 게임을 확장하지 않는다.**

## P9 — 웹 인터랙션 확장 (오브젝트)

- [ ] 물리 오브젝트 배치 (간판 · 드럼통 · 잔해)
- [ ] 웹으로 당기기 / 들기 / 던지기
- [ ] 무게 개념 — 무거우면 내가 끌려간다
- [ ] **스윙 중에도 된다.** 멈추지 않는다 (헌법 §5.4)

**완료 기준:** 스윙하다가 간판을 뜯어 던지고 그대로 스윙을 이어간다.

## P10 — 적 · 근접전 · 웹 전투 통합 ★ 레퍼런스 B 재현

- [ ] 더미 적 1~3명 + 부위 타겟 (Chest / Head / Arms / Legs)
- [ ] 웹 전투: **Pull · Yank · Bind · Slam · Leg Trip**
- [ ] 근접 격투 — 1인칭 주먹·발차기. **별도 모드가 아니다**
- [ ] 히트스톱 ≤ 100ms · 물리적 넉백 · 래그돌
- [ ] 소프트 락온 (SOFT_CONE 0.10)
- [ ] 던지기 / 벽에 처박기 / 난간 너머로
- [ ] 레퍼런스 B와 항목별 비교

**완료 기준: 스윙 → 적 끌어오기 → 주먹 → 묶기 → 뛰어내려 스윙이
모드 전환 없이 하나의 흐름으로 이어진다.** (시나리오 J)

> 이 넷이 자연스럽게 연결되기 전에는 적↔적 연계, 고급 Bind, 공중 콤보를 늘리지 않는다.

## P11 — 게임 확장

적 AI 심화 · 전투 깊이 · 튜토리얼 · 미션 · 보스 · 정식 HUD · 오디오 · 저장 ·
도시 확장 (Fab 에셋 검토) · 추가 캐릭터.

> 첫 캐릭터가 완성되기 전 여러 캐릭터 금지.

## P12 — 빌드 · 배포

Windows 빌드 · 설정 메뉴 · 성능 패스 · WebGL 검토.

---

# 10. 평가 순서 (그래픽부터 보지 않는다)

```
1.  캡슐로 스윙이 재미있는가?                      ← 지금 여기 (P2)
2.  정확한 조준 없이 웹이 의도대로 걸리는가?        (P3)
3.  좌/우 웹으로 방향 전환이 자유로운가?            (P4)
4.  벽 짚기가 자연스러운가?                        (P5)
5.  웹으로 건물을 올라가는가?                      (P5)
6.  1인칭에서 몸이 살아 있는가?                    (P6)
7.  카메라가 이를 잘 보여주는가?                   (P7)
8.  레퍼런스 A와 나란히 놓고 비슷한가?             (P8)
9.  같은 웹 시스템이 오브젝트·전투로 이어지는가?    (P9-P10)
10. 그 뒤에야 City / Model / UI를 확장한다.        (P11)
```

---

# 11. 위험 요소

| 위험 | 대응 |
|---|---|
| **Claude의 검증 루프가 유니티에서 약하다** | 감각 수학을 순수 함수로 빼서 EditMode로 검증. 유니티가 켜져 있으면 ClaudeBridge. 감각 실험은 Ver.1에서. |
| **컴파일 실패 시 옛 DLL로 테스트가 돌아 가짜 결과가 나온다** | 결과를 읽기 전 `tools/claude_compile.txt`를 항상 먼저 본다 |
| Auto Anchor 튜닝이 코드보다 어렵다 | P3에서 **시각화를 먼저** 만든다 |
| Secondary Web 과구속 | true dual constraint를 하지 않는다. Primary + 조향력 |
| 절차적 포즈가 Animator에 덮인다 | mixer 적용 **뒤**, 렌더 **전**에 얹는다 |
| 모델 교체와 이동 리팩터가 겹침 | P1~P5는 캡슐. 모델은 P6 |
| 전투를 넣다가 모드가 생긴다 | 헌법 §5.4. 새 기능마다 "이게 모드를 만드는가?"를 묻는다 |
| YAML 에셋 손상 | `.unity`/`.prefab`을 텍스트로 편집하지 않는다 |
| 1인칭 롤 멀미 | 설정으로 뺀다. 몸 기울이기가 기본 |
| OneDrive가 Library를 동기화 | 프로젝트를 OneDrive 밖으로 옮기는 것이 유일한 확실한 해법 |
| 그래픽에 시간을 쓰게 된다 | §4.7. 판단 순서는 §10 |

---

# 12. FINAL SOURCE OF TRUTH

> Ver.2의 목표는 **레퍼런스보다 자유롭게 웹을 사용하고, 플레이어의 의도를 읽어
> 실제 환경에 웹을 연결하며, 좌/우 웹과 모멘텀으로 빠르게 방향을 바꾸고,
> 벽을 손으로 짚고 이동을 이어가며, 웹으로 건물을 올라가고,
> **그 동일한 웹 시스템으로 오브젝트와 전투까지 모드 전환 없이 이어지는**
> Motion-First 1인칭 액션 게임**을 만드는 것이다.

유니티를 선택한 이유는 그래픽 때문이 아니다.
**Animation · Rigging · IK · Ragdoll · Camera 같은 기반을 유니티에 맡기고,
우리 시간을 Free Web과 Motion에 집중하기 위해서다.**

새 기능을 만들 때마다 묻는다:
> "이 기능은 플레이어가 웹을 더 자유롭고 자연스럽게 쓰게 만드는가?"
> "이 기능은 모드를 만드는가?"

구현할 때마다 묻는다:
> "지금 정상 동작하는 것을 깨지 않고 이걸 추가할 수 있는 가장 작은 단계는 무엇인가?"

---

# 부록 A — 새 세션 시작 시 첫 명령

```
C:\Users\SAMSUNG\OneDrive\Desktop\Spiderman_Ver2\00_MASTER_PLAN.md 를 읽어라.
§0을 먼저 보고 현재 상태를 파악한 뒤, 커밋 462e314에서 이식한
Ver.1 스윙 물리가 컴파일되고 테스트 37개가 통과하는지부터 확인해라.
그다음 P3(AUTO ANCHOR V2) 계획을 보고하고 승인을 기다려라.
```

# 부록 B — Ver.1 참조 방법

```bash
# 알고리즘 원본 (읽기 전용)
C:\Users\SAMSUNG\OneDrive\Desktop\spider man\src\{anchor,wallplant,vclimb,reach,mathx,fp-hands}.js

# 이미 이식한 스윙 물리의 원본 위치
game3d.js:4151-4241   상수 전부
game3d.js:6134-6222   공중 조작 · 펌프 · 드래그 · 소프트캡
game3d.js:6244-6303   로프 구속 · 자동 펌핑 · 감기 가속 · 보조 웹

# 실행해서 감각 비교
cd "C:\Users\SAMSUNG\OneDrive\Desktop\spider man" && node serve.js
# http://localhost:8173/game3d.html → "자유 이동"
# F1 도움말 · F3 앵커 디버그 · F4 앵커 V2/legacy 전환
```

# 부록 C — 레퍼런스 프레임 뽑기

```bash
# 레퍼런스 A (세로 영상이라 crop으로 가운데만 잘라야 한다 — 좌우는 SNS UI)
ffmpeg -i "스파이더맨 래퍼런스.mp4" \
  -vf "fps=3,crop=in_h*0.62:in_h*0.80:in_w*0.19:0,scale=200:-1" -q:v 5 h%04d.jpg
# 3인칭 구간 = 23.5~28초 · 건물 타기 = 37.5~44초

# 레퍼런스 B (전투 구간 20~40초)
ffmpeg -ss 20 -to 40 -i "ScreenRecording_09-07-2026%2014-43-37_1.mp4" \
  -vf "fps=3,crop=in_w*0.44:in_h:in_w*0.285:0,scale=200:-1" -q:v 4 c%03d.jpg

# 대조표
ffmpeg -i c%03d.jpg -vf "tile=10x6" -frames:v 1 -q:v 4 csheet.jpg
```

# 부록 D — Ver.1에서 실제로 겪은 함정들

| 함정 | 증상 | 원인 / 교훈 |
|---|---|---|
| 음수 스케일로 좌우 반전 | 왼팔이 기괴하게 꺾임 | `scale.x = -1`은 좌표계 손잡이를 뒤집는다. 기하로 미러링해야 한다 |
| 1인칭 몸이 카메라 뒤 | 아래를 봐도 몸이 안 보임 | z 부호 반대. 앞은 −Z |
| 시점이 몸을 따라 회전 | 멀미 | 1인칭에서 화면을 기울이면 "세상이 도는" 걸로 읽힌다. 몸만 돌린다 |
| 포즈 적용 순서 | 회전이 사라짐 | 포즈 함수가 rotation을 통째로 다시 쓴다. 적용은 그 **뒤**에 |
| 벽 짚기 고정 임펄스 | 46 m/s가 15.9로 죽음 | 속도 비례 반발로 바꿔야 한다 |
| 앵커 좌우 동점 | 어느 손이 잡을지가 우연 | 점수식에 손 편향이 없었다. 디버그 시각화가 첫 화면에서 잡아냈다 |
| 테스트 실패인데 코드는 맞음 | | 한쪽에 건물이 없는 지점이었다. 테스트 조건을 먼저 의심할 것 |
| 자동 곡예가 속도를 더함 | 물리 궤적 오염 | 연출은 궤적을 건드리면 안 된다 |

# 부록 E — Ver.2에서 실제로 겪은 함정들 (2026-09-09)

| 함정 | 증상 | 원인 / 교훈 |
|---|---|---|
| 에디터 어셈블리 정의 추가 | CS0246 URP 타입 없음 | asmdef를 만들면 참조를 전부 명시해야 한다. 예전엔 자동 참조였다 |
| 치환 스크립트로 한글 문자열 수정 | CS1010 Newline in constant | `\n` 이스케이프가 실제 줄바꿈이 됐다. **Edit 도구를 쓴다** |
| 테스트 메서드 이름을 숫자로 시작 | CS1519 | C# 식별자는 숫자로 시작할 수 없다 |
| 컴파일 실패 후 테스트 실행 | 옛 결과가 그대로 나옴 | 유니티가 직전 성공 DLL로 돌린다. `claude_compile.txt`를 먼저 본다 |
| GUI를 고정 픽셀로 그림 | QHD에서 조준점이 4픽셀 | 가상 해상도로 그린 뒤 화면에 맞춰 확대한다 |
| 로프 구속 `dist <= length` | 매번 한 스텝 늦게 잡힘 | 딱 팽팽한 순간에도 구속을 걸어야 한다. `<`가 맞다 |
| 속도가 줄과 나란할 때 | 걸리자마자 속도 0 | 접선이 0이면 방향이 없다. 의도 방향을 접평면에 투영한다 |
| 줄을 감아도 안 빨라짐 | 조작감 없음 | 길이만 줄이고 속도를 안 건드렸다. 각운동량 보존이 필요하다 |
| DROP_MAX를 수동 조준에 적용 | 아래로 못 쏨 | 그건 자동 앵커의 점수 규칙이지 플레이어를 막는 규칙이 아니다 |
| 브릿지가 응답 없음 | 6분 대기 | 유니티는 창이 포커스를 받아야 업데이트 루프가 돈다 |
